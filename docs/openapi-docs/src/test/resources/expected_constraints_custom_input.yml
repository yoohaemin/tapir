openapi: 3.0.1
info:
  title: Fruits
  version: '1.0'
paths:
  /:
    get:
      operationId: getRoot
      parameters:
        - name: color
          in: query
          required: true
          schema:
            type: integer
            minimum: '1'
      responses:
        '200':
          description: ''
          content:
            text/plain:
              schema:
                type: string
